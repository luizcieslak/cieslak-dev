---
title: Contruindo uma rádio LoFi
excerpt: Depois de ouvir muito LoFi por aí, decidi criar minha própria rádio.
publishDate: '2026-05-08'
seo:
  image:
    src: '/lofi'
    alt: A imagem clássica da "LoFi Girl" reinterpretada como se ela morasse no Brasil.
---
import RadioCallout from '../../../components/RadioCallout.astro'
import AmbientGlowSandbox from '../../../components/AmbientGlowSandbox.astro'
import AmbientPaletteSandbox from '../../../components/AmbientPaletteSandbox.astro'


![A imagem clássica da "LoFi Girl" reinterpretada como se ela morasse no Brasil.](/lofi/lofi.png)

Eu curto ouvir um LoFi. Você já provavelmente viu esse vídeo da [rádio](https://www.youtube.com/watch?v=jfKfPfyJRdk) "LoFi Girl", que tem uma menina estudando em um ambiente com músicas chamadas de "LoFi" que ajudam a se concentrar. Essa é uma live no YouTube que existe desde 2018 desde então [quase nunca](https://www.theverge.com/2020/2/24/21150488/chilledcow-lofi-hiphop-livestream-youtube-termiantion-vod-anime-study-girl) parou de tocar. A live ainda é bem popular, com pessoas interagindo constantemente no chat e pegando em média 20 mil visualizações simultâneas todos os dias.

Eu comecei a ouvir _lofi_ como algo que eu colocava de fundo na hora de estudar na universidade. Depois de me formar, também ouvia as mesmas músicas enquanto trabalhava ou precisa focar em algo. Depois de um tempo, percebi que estava ouvindo essas músicas em outras ocasiões também e colocando elas nas minhas playlists pessoais. Foi aí que eu percebi que eu gostava de uma quantidade dessas músicas e que poderia fazer uma playlist com elas.

Nesse ponto, realmente fazer _só_ uma playlist seria mais que suficiente. Porém, como um engenheiro de software que brisa um pouco demais as vezes, pensei em construir uma **rádio inteira a partir de zero**. 

Este é um post sobre o processo de construção da rádio que aprofunda em vários conceitos de matemática e programação. Todo o código que você vê aqui é _open-source_, e portanto aberto para que qualquer pessoa possa construir sua própria versão se quiser.

Antes de começar a ler esse artigo, eu lhe convido a escutar a rádio abaixo, assim podemos ouvir a música juntos.

<RadioCallout />


# Como uma rádio (lofi) pode funcionar

<picture>
	<source media="(min-width: 768px)" srcset="/lofi/diagram-h.png" />
	<img src="/lofi/diagram-v.png" alt="Diagram of how the radio is working" />
</picture>

O diagrama acima representa como a rádio que vc escuta está funcionando nesse momento. Todo o código usado está disponível nesses repositórios:

* [player]() A parte do _client_ que consome a _stream_ de aúdio e APIs via HTTP e SSE _(Server sent events)_
* [lofi-radio]() Aplicação do servidor usando Bun. Armazena os arquivos físicos, cria a _stream_ de áudio e a interface de admin.
* [shelf]() Gerenciamento de playlist local e sincronização com aplicativos de música tipo Spotify and YouTube Music.

# Client side

A parte do _client_ consome 2 APIs principais. 

* Audio _stream_ via requisição HTTP que retorna uma _stream de bytes_.
* Metadados sobre as músicas via _Server Sent Events (SSE)._ Envia eventos para o _client_ com informações sobre a música que está tocando no momento
* Metadata information about the track via Server Sent Events (SSE). It pings the client with the information about the current track being played.

Se vc colocou a rádio pra tocar ali em cima, vc já está vendo os metadados no tocador, ou do lado direito do site se está no _desktop_ ou uma faixa em baixo se está no celular.



A tag [audio](https://developer.mozilla.org/pt-BR/docs/Web/HTML/Reference/Elements/audio) é bem simples nesse momento, mas tem uma lógica em volta dele para lidar com algumas questões tipo reconexões com o servidor.

Por fim, também possui mecanismos para a tela não apagar em celulares e também usa a [MediaSession API](https://developer.mozilla.org/pt-BR/docs/Web/API/MediaSession) para transmitir as informações para os dispositivos, assim conseguimos ver o nome da música e artista no rádio do carro ou na tela do celular quando na tela de desbloqueio.

## Album cover and blur gradient

I believe artist but great thought into the album cover of their releases. I believe the intention is to see it alongside the playing track. some of them are pretty dope as well.

Thinking about that, **the most important element on the radio page is the album cover**. It is the biggest element and around it I've built a custom blur gradient effect from the colors that are extracted from the image.

The whole effect lives in [src/lib/ambient-glow/index.ts](src/lib/ambient-glow/index.ts). At a high level it samples the cover image on a small grid, picks one dominant color per cell, and paints a stack of soft radial gradients on layers sitting behind the image. Have a play with the controls below — every slider maps directly to a field of `GlowOptions` that the radio page passes into `mount()`.

<AmbientGlowSandbox />

### How colors are extracted

For each cell in the sampling grid the code calls `dominantColor()`, which is just a histogram with three escape hatches. It walks the pixel buffer four bytes at a time, drops anything that's too transparent, too dark, or too bright via a simple luminance check, and then bins what's left into coarse RGB buckets:

```ts
const lum = 0.299 * r + 0.587 * g + 0.114 * b
if (skipBlack && lum < blackThreshold) continue
if (skipWhite && lum > whiteThreshold) continue
const key = `${Math.floor(r / binSize)},${Math.floor(g / binSize)},${Math.floor(b / binSize)}`
counts.set(key, (counts.get(key) || 0) + 1)
```

The bucket with the highest count wins, and its center is reconstructed back into an `{ r, g, b }` triplet. `binSize` controls how forgiving the buckets are — large bins merge close shades into one dominant color, small bins preserve fine variation at the cost of fragmenting the vote. The black/white skips exist because letterboxing and bright backgrounds otherwise dominate the count and drain the gradient of the colors that actually matter.

Here's the same algorithm running on a 4×4 grid so you can see the palette it picks:

<AmbientPaletteSandbox />

What is crazy for me is that the exact same algorithm that can be used to pixelize an image if you go long enough increasing the grid size. 

{/* <add gif here> */}
 
Nowadays with Ultra HD 8k screens it is easy to forget the image we see in front of us in a computer is a grid of pixels, but we are actually seeing billions of them at every single frame.

# Server side


state json explanation
resilience when restarting or crash
playlist state

## something every 26ms

Not exactly every 26ms, but that number is coming from how we parse the MP3 frames.

So, a little back in history: The MP3 format itself is widely used to store and transport audio files. It is very efficient to compress an audio file size while maintaing the quality.

Inside the MP3 format, there are several ways of compressing the data. the most used of them, MPEG-1 Layer 3, is a lossy compression that is very efficient in terms of compression ratio.

(need to also explain about the 44.1kHz sampling rate)

For each frame of an audio, it stores exactly 1152 audio samples. Therefore, if the track has 44.1kHz sampling rate, and MPEG-1 Layer 3 holds 1152 samples, each frame duration is ~26ms (need fuurther explanation about the frame duration, still confusing af).

explain how a stream server works. like someone giving cards into a table. every player should have the same amount of cards at the same time. a radio transmission works like that, where you need to tune into a station and then receive the broadcast from it.

explain we need to respect that number in streaming. why setTimeout() can't be used. 
need the PreciseTimer to be able to respond in the sub-millisecond range.

setTimeout works in a best-effort way when it comes to precise milliseconds. It can't guarantee something will way to be executed in exact 26ms. It can be 26, 24, 32ms, depending how soon the CPU can handle that process. We gotta remind ourselves that server aplications are CPU-bound and therefore share the Operational System resources like any other process running alongside it.



visuals? how mp3 frame parser works, timer stuff
maybe a visualizer on each frame, dunno
stream and sse events


# Files

In this current version of the radio, it only supports MP3 files.


# Shelf for Playlist management

The playlist you can hear here is also avaible in Spotify and YouTube Music. 
This section now goes how can we use a tool named Shelf that can sync the playlist state between the apps with an ability to export it in order to file, which we use at the lofi-server.



I used spotify to search, organize and add songs to the playlist originally. Its just easier to use overall and then when we connect it to Shelf:

image1

we are able to export the playlist as a json file and also sync it songs between Spotify and other apps like YouTube Music.

image2



## How to sync the playlists between local, Spotify and YouTube Music

shelf to sync




admin page in server
shelf? maybe

lofi is cool


# how the radio is built

with code and lots of love
